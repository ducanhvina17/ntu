<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>
    <% if (typeof(data) != 'undefined' && data.name.length > 0)
    { %>
        <%= data.name %>
    <% } %>
    </title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link rel="stylesheet" href="http://code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://code.jquery.com/jquery-1.12.4.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
    <script src="/js/navsearch.js"></script>
    <script>
    GetSchool("<%= schools %>");
    </script>
</head>
<body>
    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container">
            <div id="navbar" class="collapse navbar-collapse pull-right">
                <ul class="nav navbar-nav pull-right">
                    <li class="active"><a href="/">Home</a></li>
                    <li><a href="/secondaryschool">Secondary School</a></li>
                    <li><a href="/forum">Forum</a></li>
                    <li><a href="/comparisonlist">Comparison List</a></li>
                    <% if (typeof(username) == 'undefined' || username === "")
                    { %>
                        <li><a href="/login">Login</a></li>
                    <% }
                    else
                    { %>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= username %><span class="caret"></span></a>
                            <ul class="dropdown-menu">
                                <li><a href="/favourite">Favourite List</a></li>
                                <li><a href="/userlogout">Logout</a></li>
                            </ul>
                        </li>
                    <% } %>
                    <form class="navbar-form navbar-right" action="/searchindividualschool" method="post" onsubmit="return isEmpty()">
                        <div class="form-group">
                            <input type="text" name="schoolname" id="navsearch" class="form-control navsearch" placeholder="Search">
                        </div>
                        <button type="submit" class="btn btn-default">Search</button>
                    </form>
                </ul>
            </div>
        </div>
    </nav>
    <br><br><br><br><br><br>
    <div class="container">
        <% if (typeof(data) != 'undefined' && data.name.length > 0)
        { %>
            <table class="table table-striped table-bordered resultTable">
                <tr>
                    <th><img src="/img/school/<%= data.name[0].replace(/ /g, "-") %>.jpg" height="50%"></th>
                    <th class="text-center"><%= data.name %></th>
                </tr>
                <tr>
                    <th>Location</th>
                    <td><%= data.location %></td>
                </tr>
                <tr>
                    <th>Area</th>
                    <td><%= data.area %></td>
                </tr>
                <tr>
                    <th>Telephone</th>
                    <td><%= data.telephone %></td>
                </tr>
                <tr>
                    <th>Nearest MRT</th>
                    <td><%= data.mrt %></td>
                </tr>
                <tr>
                    <th>Bus</th>
                    <td><%= data.bus %></td>
                </tr>
                <tr>
                    <th>Subjects</th>
                    <td><%= data.subject %></td>
                </tr>
                <tr>
                    <th>Route</th>
                    <td><a href="/getroute/<%= data.name[0].replace(/ /g, '+') %>" style="color: blue; font-style: italic;"><img src="../img/location.png" width="20px" height="30px"/></a></td>
                </tr>
            </table>
        <% }
        else
        { %>
            <p>No result found!</p>
        <% } %>
    </div>
</body>
</html>
