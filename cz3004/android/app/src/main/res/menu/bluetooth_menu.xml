<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/disconnect"
        android:icon="@mipmap/ic_action_bluetooth_disconnect"
        app:showAsAction="always|withText"
        android:title="disconnect"
        android:enabled="false"
        android:visible="false"/>

    <item
        android:id="@+id/rpi_select"
        android:title="@string/rpi1"
        app:showAsAction="always" />

    <item
        android:id="@+id/secure_connect_scan"
        app:showAsAction="never"
        android:title="Connect a device - Secure"/>

    <item
        android:id="@+id/insecure_connect_scan"
        android:showAsAction="never"
        android:title="Connect a device - Insecure"/>

    <item
        android:id="@+id/discoverable"
        android:showAsAction="never"
        android:title="Make discoverable"/>
</menu>